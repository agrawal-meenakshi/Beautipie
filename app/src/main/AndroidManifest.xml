<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="appdeveloper.meenakshi.com.Call_Parlour">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home_Page"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User_Registration"
            android:label="@string/title_activity_user__registration"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".Parlour_Registration"
            android:label="@string/title_activity_parlour__registration" />
        <activity
            android:name=".ParlourServices"
            android:label="@string/title_activity_parlour_services"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ParlourService"
            android:label="@string/title_activity_parlour_service"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Face_Tab"></activity>
    </application>

</manifest>